enum TypeCategory {
  // simple value type
  simple,
  // an enum
  enumeration,
  // a complex object/model
  model,
  // list of simple type entries
  simpleList,
  // a list of models or enums
  complexList
}
